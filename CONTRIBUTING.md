# 贡献说明

队友只需要修改对应分区的 Markdown 文件，不需要在本地安装 PDF、Pandoc 或 LaTeX。

## 每个模板的最低信息

请在代码前写清楚：

- 适用条件：输入模型、下标约定、是否要求连通/有向等。
- 复杂度：时间、空间和主要常数来源。
- 边界条件：空图、单点、负数、精度、容量上限等。
- 直属依赖：直接使用的结构或类型；几何模板从点、向量等最底层依赖开始逐项列出。
- 已知注意事项：与队伍现有打印模板的差异、容易误用的地方、尚未确认的内容。

说明可以让 AI 先生成草稿，但提交者必须自己核对代码和条件。代码块应尽量能直接粘贴进带 `bits/stdc++.h`、`using namespace std;` 的竞赛文件；如果不能，必须写明缺失定义。

## 提交流程

1. `git pull` 同步 `main`。
2. 只修改对应的 `docs/*.md` 分区。
3. 本地至少通读一遍，确认复杂度、边界和依赖没有胡写。
4. 推送分支并创建 PR；标题建议使用 `docs: update <section>`。

维护者会检查内容是否与本仓库定位相符、是否重复已有打印模板，以及是否有明显未声明的依赖。无需为了提交增加样例测试或复杂工作流。
